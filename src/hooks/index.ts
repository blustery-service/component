import {
  useAutocomplete as useAutocompleteHook,
  useTheme as useThemeHook,
  useMediaQuery as useMediaQueryHook,
  useColorScheme as useColorSchemeHook,
} from '@mui/material';

export const useTheme = useThemeHook;
export const useMediaQuery = useMediaQueryHook;
export const useColorScheme = useColorSchemeHook;
export const useAutocomplete = useAutocompleteHook;
